# 📚 Today I Learned (2026-02-06)

## 1. Pre-commit
- 커밋 전 자동 체크 도구 (린트, 형식 등)
- `pip install pre-commit` > `pre-commit --version` > `touch .pre-commit-config.yaml`

## 2. Git Branch 명령권
- **확인:** `git branch`
- **생성:** `git branch {name}`
- **전환:** `git switch {name}` (또는 `git switch -c {name}` 생성/전환 동시)
- **삭제:** `git branch -D {name}`

## 3. Conflict (충돌) 해결
- **Merge 시:** 충돌 파일 수정(`vi`) > `git add {file}` > `git commit`
- **Rebase 시:** `git rebase main` > 충돌 수정 > `git add` > `git rebase --continue`

---

## 4. GitHub Flow 실습 (FizzBuzz)

### 4.1. 원격 저장소 설정 (GitHub.com)
1. **New Repository:** `github_flow` (README, MIT License 포함)
2. **Issues 생성:** - `#1` 프로젝트 환경설정 (.gitignore, README)
   - `#2` fizz 구현 (3의 배수)
   - `#3` buzz 구현 (5의 배수)
   - `#4` fizzbuzz 구현 (15의 배수)

### 4.2. 로컬 환경 설정 (Terminal)

git clone {repository_url}
cd {repository_name}

- .gitignore 설정
touch .gitignore
vi .gitignore  # (macos, vim, python 설정 추가)
git add .gitignore
git commit -m "config: Create .gitignore"
git push origin main

### 4.3. 기능 구현 및 Pull Request 반영 (Full Cycle)

 1) 로컬 작업: 브랜치 생성 및 기능 개발
git switch -c feature/fizz
vi fb.py                     # 코드 작성
python3 fb.py                # 실행 확인
git add fb.py
git commit -m "feat: Add fizz function"
git push -u origin feature/fizz

 2) GitHub 웹 작업: 검토 및 합치기
 - Compare & Pull request 버튼 클릭
 - "close #n" 입력하여 이슈 연결 후 Merge
 - Merge 완료 후 Delete branch 클릭

 3) 로컬 동기화: 메인 반영 및 브랜치 삭제
git switch main              # 메인 브랜치로 복귀
git pull origin main         # 서버에 합쳐진 최신 내용 가져오기
git branch -D feature/fizz   # 할 일 끝난 로컬 브랜치 삭제

